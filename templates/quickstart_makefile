# Generated by fafafa.tui quickstart
# Adjust FTUI_ROOT if you move this project relative to fafafa.tui
FTUI_ROOT := __FTUI_ROOT__
include $(FTUI_ROOT)/fafafa_tui.mk

FPC ?= fpc
APP := __APP_NAME__
BUILD_DIR := build

FPC_FLAGS := -MObjFPC -Sh -O2 $(FTUI_FPC_FLAGS) -FE$(BUILD_DIR)

.PHONY: all run clean

all: $(BUILD_DIR)/$(APP)

$(BUILD_DIR)/$(APP): $(APP).lpr
	@mkdir -p $(BUILD_DIR)
	$(FPC) $(FPC_FLAGS) $<

run: $(BUILD_DIR)/$(APP)
	./$(BUILD_DIR)/$(APP)

clean:
	rm -rf $(BUILD_DIR)
	rm -f *.o *.ppu link*.res ppas.sh
